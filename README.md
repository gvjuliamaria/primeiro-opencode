# Frontend (React + Vite + Tailwind)

Instruções rápidas:

1. Instale dependências: `npm install` (na pasta `frontend`)
2. Rode em modo desenvolvimento: `npm run dev`
3. Build de produção: `npm run build`

Observações:
- Este é um esqueleto inicial. Envie o link do Figma com permissão de visualização e eu vou adaptar as telas, extrair assets e implementar as rotas/páginas específicas conforme o protótipo.
